pipeline {
    agent any

    stages {
        stage('Run System Info Script') {
            steps {
                sh './system-info.sh'
            }
        }
    }

    post {
        success {
            archiveArtifacts artifacts: 'report.txt', fingerprint: true
        }
    }
}